// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { CreatePlatformTenantRequestSchema } from '$lib/api/generated/schemas';
import { type CreatePlatformTenantRequest } from '$api';

/** Returns all tenants the authenticated subject is a member of. */
export const getTenants = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.platform.getTenants();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in platform.getTenants:', err);
    throw error(500, 'Failed to get tenants');
  }
});

/** Creates a new tenant with the authenticated subject as owner.
Requires MultitenancyConfiguration.AllowSelfServiceCreation to be enabled. */
export const createTenant = command(CreatePlatformTenantRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.platform.createTenant(request as CreatePlatformTenantRequest);
    await Promise.all([
      getTenants(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in platform.createTenant:', err);
    throw error(500, 'Failed to create tenant');
  }
});

/** Returns the current multitenancy configuration status.
Used by the frontend to display subdomain URLs and transition notices. */
export const getTransitionStatus = query(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.platform.getTransitionStatus();
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in platform.getTransitionStatus:', err);
    throw error(500, 'Failed to get transition status');
  }
});
