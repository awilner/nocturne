// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { SetupTenantRequestSchema, SetupOwnerOptionsRequestSchema, SetupOwnerCompleteRequestSchema, SetupOwnerOidcRequestSchema } from '$lib/api/generated/schemas';
import { type SetupTenantRequest, type SetupOwnerOptionsRequest, type SetupOwnerCompleteRequest, type SetupOwnerOidcRequest } from '$api';

/** Create the first tenant on a fresh install. Only succeeds when zero tenants exist. */
export const createTenant = command(SetupTenantRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.setup.createTenant(request as SetupTenantRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in setup.createTenant:', err);
    throw error(500, 'Failed to create tenant');
  }
});

/** Generate passkey registration options for the first owner account.
Guard: exactly one tenant must exist with zero non-system members. */
export const ownerOptions = command(SetupOwnerOptionsRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.setup.ownerOptions(request as SetupOwnerOptionsRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in setup.ownerOptions:', err);
    throw error(500, 'Failed to owner options');
  }
});

/** Complete passkey registration for the first owner account.
Verifies attestation, generates recovery codes, issues a full JWT session. */
export const ownerComplete = command(SetupOwnerCompleteRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.setup.ownerComplete(request as SetupOwnerCompleteRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in setup.ownerComplete:', err);
    throw error(500, 'Failed to owner complete');
  }
});

/** Initiate OIDC-based owner creation. Creates the subject and owner role,
then redirects to the OIDC provider to link an identity. */
export const ownerOidc = command(SetupOwnerOidcRequestSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.setup.ownerOidc(request as SetupOwnerOidcRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in setup.ownerOidc:', err);
    throw error(500, 'Failed to owner oidc');
  }
});
