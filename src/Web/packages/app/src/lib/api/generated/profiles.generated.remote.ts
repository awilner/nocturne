// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command, form } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { TherapySettingsSchema, BasalScheduleSchema, CarbRatioScheduleSchema, SensitivityScheduleSchema, TargetRangeScheduleSchema } from '$lib/api/generated/schemas';
import { type TherapySettings, type BasalSchedule, type CarbRatioSchedule, type SensitivitySchedule, type TargetRangeSchedule } from '$api';

/** Get a consolidated summary of all profile data across all profile names.
Optionally provide a date range to include schedule change detection info. */
export const getProfileSummary = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getProfileSummary(params?.from, params?.to);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getProfileSummary:', err);
    throw error(500, 'Failed to get profile summary');
  }
});

/** Get all therapy settings with optional filtering */
export const getTherapySettings = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional(), limit: z.number().optional(), offset: z.number().optional(), sort: z.string().optional(), device: z.string().optional(), source: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getTherapySettings(params?.from, params?.to, params?.limit, params?.offset, params?.sort, params?.device, params?.source);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getTherapySettings:', err);
    throw error(500, 'Failed to get therapy settings');
  }
});

/** Create a new therapy settings record */
export const createTherapySettings = form(TherapySettingsSchema as any, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.createTherapySettings(request as TherapySettings);
    await Promise.all([
      getProfileSummary(undefined).refresh(),
      getTherapySettings(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.createTherapySettings:', err);
    throw error(500, 'Failed to create therapy settings');
  }
});

/** Get therapy settings by profile name */
export const getTherapySettingsByName = query(z.string(), async (profileName) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getTherapySettingsByName(profileName);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getTherapySettingsByName:', err);
    throw error(500, 'Failed to get therapy settings by name');
  }
});

/** Get a therapy settings record by ID */
export const getTherapySettingsById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getTherapySettingsById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getTherapySettingsById:', err);
    throw error(500, 'Failed to get therapy settings by id');
  }
});

/** Update an existing therapy settings record */
export const updateTherapySettings = form(z.object({ id: z.string(), request: TherapySettingsSchema }) as any, async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.updateTherapySettings(id, request as TherapySettings);
    await Promise.all([
      getProfileSummary(undefined).refresh(),
      getTherapySettings(undefined).refresh(),
      getTherapySettingsById(id).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.updateTherapySettings:', err);
    throw error(500, 'Failed to update therapy settings');
  }
});

/** Delete a therapy settings record */
export const deleteTherapySettings = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.profile.deleteTherapySettings(id);
    await Promise.all([
      getProfileSummary(undefined).refresh(),
      getTherapySettings(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.deleteTherapySettings:', err);
    throw error(500, 'Failed to delete therapy settings');
  }
});

/** Get basal schedules by profile name */
export const getBasalSchedulesByName = query(z.string(), async (profileName) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getBasalSchedulesByName(profileName);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getBasalSchedulesByName:', err);
    throw error(500, 'Failed to get basal schedules by name');
  }
});

/** Get a basal schedule by ID */
export const getBasalScheduleById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getBasalScheduleById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getBasalScheduleById:', err);
    throw error(500, 'Failed to get basal schedule by id');
  }
});

/** Create a new basal schedule */
export const createBasalSchedule = form(BasalScheduleSchema as any, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.createBasalSchedule(request as BasalSchedule);
    await Promise.all([
      getProfileSummary(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.createBasalSchedule:', err);
    throw error(500, 'Failed to create basal schedule');
  }
});

/** Update an existing basal schedule */
export const updateBasalSchedule = form(z.object({ id: z.string(), request: BasalScheduleSchema }) as any, async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.updateBasalSchedule(id, request as BasalSchedule);
    await Promise.all([
      getProfileSummary(undefined).refresh(),
      getBasalScheduleById(id).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.updateBasalSchedule:', err);
    throw error(500, 'Failed to update basal schedule');
  }
});

/** Delete a basal schedule */
export const deleteBasalSchedule = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.profile.deleteBasalSchedule(id);
    await Promise.all([
      getProfileSummary(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.deleteBasalSchedule:', err);
    throw error(500, 'Failed to delete basal schedule');
  }
});

/** Get carb ratio schedules by profile name */
export const getCarbRatioSchedulesByName = query(z.string(), async (profileName) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getCarbRatioSchedulesByName(profileName);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getCarbRatioSchedulesByName:', err);
    throw error(500, 'Failed to get carb ratio schedules by name');
  }
});

/** Get a carb ratio schedule by ID */
export const getCarbRatioScheduleById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getCarbRatioScheduleById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getCarbRatioScheduleById:', err);
    throw error(500, 'Failed to get carb ratio schedule by id');
  }
});

/** Create a new carb ratio schedule */
export const createCarbRatioSchedule = command(CarbRatioScheduleSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.createCarbRatioSchedule(request as CarbRatioSchedule);
    await Promise.all([
      getProfileSummary(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.createCarbRatioSchedule:', err);
    throw error(500, 'Failed to create carb ratio schedule');
  }
});

/** Update an existing carb ratio schedule */
export const updateCarbRatioSchedule = command(z.object({ id: z.string(), request: CarbRatioScheduleSchema }), async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.updateCarbRatioSchedule(id, request as CarbRatioSchedule);
    await Promise.all([
      getProfileSummary(undefined).refresh(),
      getCarbRatioScheduleById(id).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.updateCarbRatioSchedule:', err);
    throw error(500, 'Failed to update carb ratio schedule');
  }
});

/** Delete a carb ratio schedule */
export const deleteCarbRatioSchedule = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.profile.deleteCarbRatioSchedule(id);
    await Promise.all([
      getProfileSummary(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.deleteCarbRatioSchedule:', err);
    throw error(500, 'Failed to delete carb ratio schedule');
  }
});

/** Get sensitivity schedules by profile name */
export const getSensitivitySchedulesByName = query(z.string(), async (profileName) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getSensitivitySchedulesByName(profileName);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getSensitivitySchedulesByName:', err);
    throw error(500, 'Failed to get sensitivity schedules by name');
  }
});

/** Get a sensitivity schedule by ID */
export const getSensitivityScheduleById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getSensitivityScheduleById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getSensitivityScheduleById:', err);
    throw error(500, 'Failed to get sensitivity schedule by id');
  }
});

/** Create a new sensitivity schedule */
export const createSensitivitySchedule = command(SensitivityScheduleSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.createSensitivitySchedule(request as SensitivitySchedule);
    await Promise.all([
      getProfileSummary(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.createSensitivitySchedule:', err);
    throw error(500, 'Failed to create sensitivity schedule');
  }
});

/** Update an existing sensitivity schedule */
export const updateSensitivitySchedule = command(z.object({ id: z.string(), request: SensitivityScheduleSchema }), async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.updateSensitivitySchedule(id, request as SensitivitySchedule);
    await Promise.all([
      getProfileSummary(undefined).refresh(),
      getSensitivityScheduleById(id).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.updateSensitivitySchedule:', err);
    throw error(500, 'Failed to update sensitivity schedule');
  }
});

/** Delete a sensitivity schedule */
export const deleteSensitivitySchedule = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.profile.deleteSensitivitySchedule(id);
    await Promise.all([
      getProfileSummary(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.deleteSensitivitySchedule:', err);
    throw error(500, 'Failed to delete sensitivity schedule');
  }
});

/** Get target range schedules by profile name */
export const getTargetRangeSchedulesByName = query(z.string(), async (profileName) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getTargetRangeSchedulesByName(profileName);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getTargetRangeSchedulesByName:', err);
    throw error(500, 'Failed to get target range schedules by name');
  }
});

/** Get a target range schedule by ID */
export const getTargetRangeScheduleById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.profile.getTargetRangeScheduleById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.getTargetRangeScheduleById:', err);
    throw error(500, 'Failed to get target range schedule by id');
  }
});

/** Create a new target range schedule */
export const createTargetRangeSchedule = command(TargetRangeScheduleSchema, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.createTargetRangeSchedule(request as TargetRangeSchedule);
    await Promise.all([
      getProfileSummary(undefined).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.createTargetRangeSchedule:', err);
    throw error(500, 'Failed to create target range schedule');
  }
});

/** Update an existing target range schedule */
export const updateTargetRangeSchedule = command(z.object({ id: z.string(), request: TargetRangeScheduleSchema }), async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.profile.updateTargetRangeSchedule(id, request as TargetRangeSchedule);
    await Promise.all([
      getProfileSummary(undefined).refresh(),
      getTargetRangeScheduleById(id).refresh()
    ]);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.updateTargetRangeSchedule:', err);
    throw error(500, 'Failed to update target range schedule');
  }
});

/** Delete a target range schedule */
export const deleteTargetRangeSchedule = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.profile.deleteTargetRangeSchedule(id);
    await Promise.all([
      getProfileSummary(undefined).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { url } = getRequestEvent(); throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, 'Forbidden');
    console.error('Error in profile.deleteTargetRangeSchedule:', err);
    throw error(500, 'Failed to delete target range schedule');
  }
});
